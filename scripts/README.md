# scripts

Deterministic operational utilities: seed generation, imports/exports, maintenance, development utilities (CLAUDE.md §6).

No scripts exist yet in Phase 0. Reusable business logic must not live only here — extract it into `apps/api/app/domain` or `apps/api/app/services` and have scripts call into that, not the other way around.
