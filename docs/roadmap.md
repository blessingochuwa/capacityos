# Roadmap

This is the single place to see what CapacityOS has built and what's genuinely still open. **Completed phases** (0–27) are drawn directly from CLAUDE.md §39 and their ADRs — that section is the authoritative build order and this table should never drift from it. **Proposed future phases** are compiled from every deferral CLAUDE.md and the ADRs already named explicitly (§22 external integrations, §23 Chrome extension, and the "Deferred items" paragraph at the end of CLAUDE.md §39) — nothing below was invented for this document. Their numbering and grouping are a proposal, not a commitment: this project's own history (see Phase 13's ADR) is that "what phase comes next" gets decided deliberately, by asking, not by assuming a pre-written list — treat anything below "Completed phases" as provisional until CLAUDE.md §39 itself is amended to confirm it, the same way Phases 9–20 were each confirmed as they happened.

## Status legend

| Status | Meaning |
|---|---|
| ✅ Complete | Shipped, tested, documented, has an ADR (except where noted) |
| 🚧 v1 shipped | A deliberately reduced first slice is complete; a named, scoped remainder is proposed below, not dropped |
| 🔜 Proposed next | Named by name in this doc's most recent revision as the next thing to build |
| 📋 Proposed, unscheduled | A real, named gap — not yet ordered or confirmed |

## Completed phases (0–45)

| Phase | Name | Key deliverable | ADR |
|---|---|---|---|
| 0 | Repository & architecture bootstrap | Monorepo structure, tooling, CI | [0001](adr/0001-phase-0-bootstrap.md) |
| 1 | Domain foundation | People, Teams, Projects, Availability, Allocations | [0002](adr/0002-phase-1-domain-foundation.md) |
| 2 | Deterministic capacity engine | `calculate_period_capacity`, working-hours/availability/allocation math, zero AI in the calculation path | [0003](adr/0003-phase-2-capacity-engine.md) |
| 3 | Core dashboard & planning views | Read-only team/person/project capacity views, consuming Phase 2 endpoints verbatim | — |
| 4 | Scenario planning | Hypothetical what-if operations over a baseline, no mutation of live data | [0004](adr/0004-phase-4-scenario-planning.md) |
| 5 | Operational insights | Deterministic, explainable decision-support signals (over-allocation, capacity risk, concentration, imbalance, pressure, scenario deltas) | [0005](adr/0005-phase-5-operational-insights.md) |
| 6 | Import / export | CSV/JSON portability, two-stage validate-then-apply | [0006](adr/0006-phase-6-import-export.md) |
| 7 | Skills & bottleneck analysis | Skill vs. total capacity, qualified-capacity/coverage, skill-gap signals plugged into Phase 5's pipeline | [0007](adr/0007-phase-7-skills-bottleneck-analysis.md) |
| 8 | AI insight layer | Provider-abstracted interpretation strictly downstream of the deterministic engine; app works fully with no AI configured | [0008](adr/0008-phase-8-ai-insight-layer.md) |
| 9 | Production readiness & observability | Structured logging, request correlation, health/readiness, config safety gate | [0009](adr/0009-phase-9-production-readiness.md) |
| 10 | Authentication, RBAC & audit | Session-cookie login, five-role permission model, persistent append-only audit trail | [0010](adr/0010-authentication-rbac-audit.md) |
| 11 | Instance-level resource authorization | `TeamAccessGrant`/`ProjectAccessGrant` — a Manager's write access is grant-scoped, not automatic org-wide | [0011](adr/0011-instance-level-resource-authorization.md) |
| 12 | Organizations & multi-tenancy | Every entity belongs to exactly one Organization; role moves to `OrganizationMembership`; cross-org access 404s, never 403s | [0012](adr/0012-organizations-multi-tenancy.md) |
| 13 | Risk management | Project-scoped risk register, derived (never stored) exposure, two new Insights signal types | [0013](adr/0013-phase-13-risk-management.md) |
| 14 | Stakeholder management | Project-scoped stakeholder register, influence/interest/decision-authority stored but never scored | [0014](adr/0014-phase-14-stakeholder-management.md) |
| 15 | Last-owner invariant | Every active Organization retains ≥1 Owner who can actually authenticate; closes a Phase 12 gap; atomic-guarded-UPDATE concurrency fix | [0015](adr/0015-last-owner-invariant.md) |
| 16 | Instance-authorization completion | Audited every remaining Phase 11 deferral (Team→Project inheritance, Person-keyed scoping, Scenario scoping) and deliberately retained each; closed a real cross-org test-coverage gap instead | [0016](adr/0016-instance-authorization-completion.md) |
| 17 🚧 | Prioritization engine (v1 slice) | RICE + Weighted Scoring rank a portfolio against an organization-chosen framework; a score is always derived at read time, never stored. First phase preceded by a [PRD](PRD-phase-17-prioritization.md), confirmed with the user before implementation. | [0017](adr/0017-prioritization-engine.md) |
| 18 🚧 | Prioritization frameworks & dependencies (Phase 17b slice) | ICE/WSJF/MoSCoW formulas complete the framework set; a Weighted Scoring framework's criteria can be edited after creation; `ProjectDependency` (blocks/related/enables) with cycle detection, plus a Dependency Graph view. | [0018](adr/0018-prioritization-frameworks-and-dependencies.md) |
| 19 🚧 | AI priority explanation | A fifth Phase 8 AI capability (`explain-priority`), reusing `AIContextBuilder`/`AIService`/grounding unchanged — explains an existing `ProjectPriorityScore` without ever recalculating it. | [0019](adr/0019-ai-priority-explanation.md) |
| 20 🚧 | Scenario-vs-baseline prioritization comparison | Resolves the Phase 19-flagged product decision: a Scenario can declare explicit, hypothetical criterion overrides (never auto-derived from capacity data); baseline and scenario rankings are both computed through the unchanged Phase 17/18 scoring engine and diffed. | [0020](adr/0020-scenario-priority-comparison.md) |
| 21 | Portfolio snapshots | An explicit, user-triggered, immutable point-in-time saved ranking (the PRD's own original §8 proposal). `PortfolioSnapshotService.create` freezes `ProjectPriorityScoreService.rank_portfolio`'s result verbatim — framework name/type and every entry's project name/score/rank/breakdown — so a later rename, re-score, or deletion never retroactively changes an already-taken snapshot. No PATCH/DELETE — immutable and append-only, matching `AuditEvent`. | [0021](adr/0021-portfolio-snapshots.md) |
| 22 | Portfolio snapshot diff/trend | Compares two immutable Phase 21 snapshots — entered/left/changed/unchanged per project, same-framework-only (rejected with 422 otherwise). Pure computation over already-frozen data (`app/domain/portfolio_snapshot.py::compare_snapshot_entries`) — no scoring engine involved, nothing persisted, 0 new tables. | [0022](adr/0022-portfolio-snapshot-comparison.md) |
| 23 | AI snapshot comparison explanation | A sixth Phase 8 AI capability (`explain-snapshot-comparison`), reusing `AIContextBuilder`/`AIService`/grounding unchanged — explains an existing Phase 22 snapshot comparison without ever recalculating its status/rank/score/category. | [0023](adr/0023-ai-snapshot-comparison-explanation.md) |
| 24 | Multi-snapshot portfolio trend visualization | A score-over-time line chart across 2+ Phase 21 snapshots, built entirely from the existing `GET /api/v1/prioritization/snapshots` response — 0 backend changes. `buildSnapshotTrend` (frontend, pure, unit-tested) never recomputes a score and never fabricates a value for a project absent from a snapshot. A rank-over-time variant was audited and explicitly not selected (a MoSCoW score/rank is always null; rank conflates a project's own change with the portfolio around it). | [0024](adr/0024-portfolio-snapshot-trend.md) |
| 25 | WSJF breakdown visualization | One of the PRD's own five §15 Recharts visualizations. Built entirely from the existing `GET /api/v1/prioritization/portfolio` response — 0 backend changes. `buildWsjfBreakdown` copies each of WSJF's four fixed criteria verbatim from `breakdown`; the three additive Cost-of-Delay components are stacked, `job_size` (the formula's divisor, not a fourth additive term) renders as its own adjacent bar. The other four PRD visualizations were audited and found either ambiguous (Capacity-vs-Priority, Risk-vs-Value) or still blocked (dependency timeline). | [0025](adr/0025-wsjf-breakdown-visualization.md) |
| 26 | AI scenario-vs-baseline prioritization comparison explanation | A seventh Phase 8 AI capability (`explain-scenario-priority-comparison`), reusing `AIContextBuilder`/`AIService`/grounding unchanged — explains an existing Phase 20 baseline-vs-scenario comparison without ever recalculating its score/rank/category. The item Phase 20's own brief explicitly named as intentionally deferred; re-verified against current code that every other deferred candidate remained blocked before selecting this one, so no blocking question was needed. | [0026](adr/0026-ai-scenario-priority-comparison-explanation.md) |
| 27 | Priority vs. Effort scatter visualization | Another of the PRD's own five §15 Recharts visualizations. Built entirely from the existing `GET /api/v1/prioritization/portfolio` response — 0 backend changes. `buildPriorityEffortScatter` plots each RICE/WSJF-scored project's already-computed score against its effort-like criterion value (`effort` for RICE, `job_size` for WSJF — the only two frameworks with a code-defined effort concept; ICE's formula has no denominator at all). No quadrant lines or "quick win" threshold — CLAUDE.md §17/§29 forbid inventing one for display. See "Proposed next" below for the still-named remainder. | [0027](adr/0027-priority-effort-scatter-visualization.md) |
| 28 🚧 | Membership management UI (first slice) | The item every audit since Phase 22 named as "backend-ready, deferred for size only" — the only non-blocked candidate. A new `/admin/members` page (`apps/web/src/features/members/`): the active organization's member roster (active + revoked), inline role change, revoke, reactivate, add an existing account by email. **0 backend changes** — every route (`app/api/v1/organizations.py`) already exists, is `MEMBERSHIP_MANAGE`-gated, organization-scoped, audited, and enforces the Owner-escalation rule (403) and the Phase 15 last-Owner invariant (422); the frontend surfaces those responses inline and re-derives no authorization (CLAUDE.md §21), mirroring `AccessManagementPage`. First-slice boundary confirmed with the user. Deliberately excludes `User`-account create/disable and org rename/deactivate. | [0028](adr/0028-membership-management-ui.md) |
| 29 🚧 | User account management UI | The companion slice Phase 28 named as backend-ready and excluded. A new `/admin/users` page (`apps/web/src/features/users/`): create an account (email, password ≥10 chars, display name, optional link to a Person in the acting org), disable one (behind the existing inline confirm pattern), re-enable a disabled/invited one. **0 backend changes** — `POST`/`PATCH`/`GET /api/v1/users` already exist, gated by `USER_WRITE`/`USER_READ` (Admin/Owner); disable already routes through the Phase 15 last-Owner guard (422, rendered verbatim). `User` management is **global** and permission-gated, **not** organization-scoped (`GET /users` is a cross-org account directory, ADR 0012 Decision 8) — the only org-scoped element is the optional `person_id` link. Deliberately excludes org rename/deactivate, invitations, email verification, password reset, SSO, billing, directory search. | [0029](adr/0029-user-account-management-ui.md) |
| 30 🚧 | Organization settings UI (rename only) | The last backend-ready admin surface. A new `/admin/organization` page (`apps/web/src/features/organization/`): shows the current organization's name, immutable slug, and status; renames via the existing `PATCH /api/v1/organizations/{id}` (`{name: 1..200}`). **0 backend production changes** (5 test-only additions to `tests/api/test_organizations.py` for the previously-uncovered `GET`/`PATCH` contract). Gated by `can('organization.manage')` — **Owner-only**; a path id that isn't the caller's active org 404s. **Deactivation deliberately deferred by product decision** (ADR 0030): `OrganizationService.deactivate` has no cascade, no backend guard, and no reactivation path — irreversible through the product, locks out every member including the acting Owner. | [0030](adr/0030-organization-settings-ui.md) |
| 31 | Organization deactivation safety & reactivation (backend) | Makes the deferred deactivation lifecycle safe and reversible so a future Phase 32 UI can consume it. **Safety guard**: an org may be deactivated only with **≥ 2 active Owners** (atomic guarded `UPDATE`, `OrganizationRepository.deactivate_if_safe`, mirroring Phase 15) — otherwise 422; a sole-Owner org cannot be deactivated until a second Owner exists. **Reactivation**: new `POST /api/v1/organizations/{id}/reactivate` (CSRF-protected, authorized by resolving the caller's Owner membership in the target org directly — like `switch_organization` — since a deactivated org has no active-membership context). Flips only `is_active`; no cascade; identity/relationships preserved; idempotent. New `AuditAction.ORGANIZATION_REACTIVATE` (open-vocab `String` → **no migration**). Session/switching behavior unchanged. Concurrency proven against a real file-backed SQLite DB. **0 tables, 0 migrations, 0 new permissions/roles, 0 frontend changes.** | [0031](adr/0031-organization-deactivation-safety.md) |
| 32 | Organization deactivation / reactivation UI | **Frontend only** (0 backend files changed), consuming the Phase 31 contract. On the existing Owner-gated `/admin/organization` page: a **Deactivation section** (two-step inline confirm, no modal primitive) → `POST .../deactivate`, and an **`InactiveOrganizationPanel`** recovery surface → `POST .../reactivate`, shown when `GET /organizations/{id}` returns the 409 "no longer active" state (scoped detection — no global 409 reinterpretation). The frontend performs **no** safety logic — it never computes the ≥2-Owner count (no usable endpoint exposes it) and surfaces the backend's 422/403/404 **verbatim**. On success the hooks `invalidateQueries()`; no forced logout, no re-login (Phase 31 behavior). Deferred to Phase 33: a global inactive-org banner (needs a read-only `OrganizationSummary.is_active`) and pruning deactivated orgs from the `OrganizationSwitcher`. | [0032](adr/0032-organization-deactivation-reactivation-ui.md) |
| 33 | Global inactive-organization awareness & switcher cleanup | Closes the two Phase 32 limitations. **Backend: 1 field** — `OrganizationSummary` (the org shape in `/auth/me`) gains a read-only `is_active: bool`, straight from the persisted `Organization.is_active` via the existing `model_validate` (**no migration** — column exists since Phase 12; no new endpoint/permission/role). **Frontend:** a persistent shell-level `InactiveOrganizationBanner` (`role="alert"`, not a modal/toast) shown when `user.active_organization.is_active === false` — Owner → link to the existing `/admin/organization` recovery panel; non-Owner → "ask an Owner." `OrganizationSwitcher`/`SelectOrganizationPage` filter out deactivated orgs (the switcher keeps the current one, labelled "(inactive)") — UX only; `switch-organization` already 404s an inactive org. Phase 32's `invalidateQueries()` already refetches `/auth/me`, so the banner appears/clears with no re-login. Phase 32's scoped 409 handling is untouched (no global 409 interceptor). | [0033](adr/0033-global-inactive-organization-awareness.md) |
| 34 | Account directory search & filtering | The account-directory search/filter box Phase 29 named as its own known limitation. `GET /api/v1/users` gains two optional query params — `q` (case-insensitive substring over email/display_name, via SQLAlchemy's portable `.ilike()`) and `status` (exact match against `UserStatus`) — both filtered in SQL (`UserRepository.list_filtered`), never fetched-then-filtered in Python. **0 new tables, 0 migrations, 0 new permissions.** `USER_READ`/global-directory scoping (ADR 0012 Decision 8) unchanged; the separate `USER_WRITE` org-scoping product decision (still open at the time) was deliberately left untouched. Frontend: a new `UsersFilterBar` (search input + status `Select`) on the existing `/admin/users` page, 300ms-debounced search, both filters threaded through the React Query key so a filter change is a distinct cached query, never a client-side re-filter. Found and fixed one correctness interaction along the way: the create-account form's "eligible People" picker was recomputed from an always-unfiltered second query so an active search/status filter can never hide an already-linked Person from it. No pagination-UI redesign — no page-index state exists anywhere in this app yet (every list fetches up to 500 rows in one request), so there was nothing to reconcile. | [0034](adr/0034-account-directory-search-filter.md) |
| 35 | USER_WRITE organization-scoping decision | Resolves the item ADR 0029 explicitly flagged as an open question and every phase since (30–34) repeated as still open: can an Admin/Owner of Organization A rename or disable/enable a `User` account with no membership in Organization A? Audited first for an existing decision — none found; CLAUDE.md §27's "don't modify another organization's data" principle was considered and found not dispositive, since `User` is explicitly modeled (ADR 0012) as belonging to no organization at all. Three options were put to the user (keep global / scope both rename and status to the caller's org / scope only status); **the user chose to keep `USER_WRITE` global** — the pre-existing, already-tested behavior. **0 production code changes.** What Phase 35 closes is the ambiguity itself: the property is now a documented, deliberate decision, locked in by one new regression test proving an Admin/Owner can mutate an account whose only membership is in a different organization, verified live over real HTTP with a control check confirming the *already*-organization-scoped membership/role surface (unaffected by this decision) still correctly 404s cross-organization. | [0035](adr/0035-user-write-global-scope.md) |
| 36 | Import/export registration for Risk, Stakeholder & Prioritization | Closes the gap ADR 0013/0014 explicitly deferred and the roadmap's own long-standing "no natural identity key" blocking question. Audited the existing pipeline first: a single central registry (`ImportEntityType` + `ENTITY_COLUMNS` + per-entity normalize functions, zero new routes ever needed) with three established identity-matching shapes to choose from per entity. **Risk** gets a new nullable `external_id` column (mechanical application of the existing "no natural key → add external_id" precedent — 1 migration). **Stakeholder** needed no schema change — its existing `(project_id, person_id)` unique constraint already supplies a natural key for person-linked rows; a person-less row always creates. **Prioritization** registers `ProjectPriorityScore`/`ProjectPriorityCriterionValue` (the actual human-entered source data, `PRIORITIZATION_SCORE`-gated, Manager+) — **`PrioritizationFramework` was deliberately excluded**, since it's `PRIORITIZATION_MANAGE`-gated (Admin/Owner only) and registering it into the Manager+ import pipeline would be a real privilege escalation, the one genuine authorization boundary this audit found. Frontend: the existing entity-type picker list and export scope-field mapping both gained three entries; every other component is generic and needed no change. **1 migration, 0 new permissions, 0 new routes.** Verified live, including a call to the unchanged portfolio-ranking route confirming imported values feed the existing scoring engine unmodified. | [0036](adr/0036-import-export-risk-stakeholder-prioritization.md) |
| 37 | Import/export registration for ProjectDependency; PortfolioSnapshot deliberately deferred | Completes the two entities Phase 36 left outside its own scope. **ProjectDependency**: its existing `(from_project_id, to_project_id, dependency_type)` unique constraint already supplied a complete natural key — **0 migrations**. No mutable fields at all (create/remove only, matching TeamMembership's exact shape), so import has no update case. Self-dependency checked per-row; `BLOCKS`-only cycle detection needs cross-row batch state (a later row must see a cycle an earlier row in the same file already closes), built as a structural mirror of WorkingSchedule's overlap pre-check and reusing the same `detects_cycle` function the direct API already calls — never a second implementation. **PortfolioSnapshot was deliberately NOT registered, on either side.** Its own model docstring settles import ("deliberately NOT read back as an input to any live computation," immutable/append-only like AuditEvent, and its `create()` takes only a `framework_id` — no user-supplied content exists to import). Export was evaluated independently but the existing architecture offers no way to expose it without also exposing an unguarded, crash-prone import path (`/imports/{type}` and `/exports/{type}` share one enum with no per-route capability flag) — closing that gap would be new infrastructure, explicitly out of scope. **0 migrations, 0 new permissions, 0 new routes, 0 changes to PortfolioSnapshot.** Verified live, including a real cross-row cycle check and cross-organization isolation. | [0037](adr/0037-import-export-project-dependency.md) |
| 38 | PortfolioSnapshot export decision — Outcome A | Resolves the export half of the question Phase 37 deferred to its own dedicated audit. Five independently converging pieces of evidence were checked directly: `AuditEvent` — the entity PortfolioSnapshot's own docstring explicitly models itself after — has never been exportable anywhere in this codebase; the PRD describes the purpose as in-app "trend/history," never export/download/backup; ADR 0006's founding charter is about operational source-data portability for onboarding, not a derived, after-the-fact artifact; the specific "see the trend" need is already shipped (Phase 24's chart, zero backend changes); and a full grep of every frontend file consuming PortfolioSnapshot found zero export signal. **No capability architecture (`importable`/`exportable`/`both`) was built** — it would have been sized for one entity this same audit found doesn't need either capability. **0 production code changes, 0 migrations, 0 new permissions, 0 API contract changes, 0 frontend changes.** Two regression tests lock the decision in as intentional (both asserting FastAPI's own enum-path-parameter 422, not a special-cased rejection). Verified live against a fresh database. | [0038](adr/0038-portfoliosnapshot-export-capability.md) |
| 39 | Prioritization visualization audit — hard stop | Audit-only, **no code, no ADR, no commit**. Re-verified directly against current source that all three then-remaining PRD §15 visualizations are blocked by genuine, unresolved product decisions (Capacity-vs-Priority: "a project's capacity" is undefined; Risk-vs-Value: `Risk` is a project-scoped *set* with no specified aggregation, and "Value" is not a defined term; dependency timeline: narrowest — `Project.start_date`/`end_date` exist, so the gap is "what does the axis plot / how are undated projects handled," not missing data). Recommended the dependency timeline as the next increment. | — |
| 40 | Dependency timeline visualization | The PRD's own §15 dependency timeline, built with **zero backend changes** (`apps/web/src/features/prioritization/utils/dependencyTimeline.ts::buildDependencyTimeline`, `components/DependencyTimeline.tsx`, one new card on `PrioritizationOverviewPage`): every project with **both** a `start_date` and an `end_date` placed on a shared date axis (a horizontal Recharts range-bar Gantt, `aria-hidden`, paired with an accessible table — the `ProjectDemandTimeline`/`PriorityEffortScatterChart` precedent), plus a table of the `blocks` relationships between them. Built from `GET /api/v1/projects` (already `PROJECT_READ`-gated, active-org-scoped) joined client-side with the existing, unchanged `GET /api/v1/prioritization/dependency-graph` — no new endpoint/model/migration/permission. Phase 39's product decisions implemented verbatim: a project missing **either** date goes to a separate **"Unscheduled projects"** list, never given a fabricated date or position; only `blocks` edges are drawn (`related`/`enables` excluded; `blocked_by` not synthesised); **no schedule-consistency judgement is computed** (CLAUDE.md §17/§29); the view is entirely read-only. `buildDependencyTimeline` is pure and unit-tested — copies each date verbatim, sorts deterministically, reflects only the projects it is given. +19 frontend tests (322 → 341). | [0040](adr/0040-dependency-timeline.md) |
| 41 | Full roadmap re-audit & next-increment selection | **Audit-only, no code, no ADR, no commit.** Re-verified every roadmap claim directly against current source (models, routers, enums, route tables, frontend routes/nav) rather than trusting prior ADRs, reconfirmed Capacity-vs-Priority and Risk-vs-Value both still blocked, and searched beyond the named-candidate list for smaller ready gaps — finding `GET /api/v1/audit` (built since Phase 10, `AUDIT_READ`-gated) had zero frontend consumer anywhere. Recommended an Audit Log UI as the strongest ready Phase 42 candidate. | — |
| 42 | Capacity vs. Priority matrix visualization | The PRD's own §15 Capacity-vs-Priority matrix, unblocked by an explicit product decision supplied directly by the user (not derived by audit): a project's capacity requirement is the total `Allocation.allocation_hours` recorded against it. Built with **zero backend changes** (`utils/capacityPriorityMatrix.ts::buildCapacityPriorityMatrix`, `components/CapacityPriorityMatrixChart.tsx`, `hooks/useAllocations.ts`, one new nested query inside the existing "Portfolio priority board" card): each project scored under the selected framework plotted by its summed allocation hours (X) against its already-computed priority score (Y, copied verbatim from `GET /api/v1/prioritization/portfolio`), joined client-side with `GET /api/v1/allocations` (a new filter-free `allocationsApi.list()` wrapper over the existing, unchanged, org-scoped route). Median-based reference lines (never an invented threshold) with a documented "equal-to-median is the low side" tie rule; purely descriptive quadrant labels, never a recommendation. A project with zero recorded allocations is excluded as missing capacity data, never plotted at a fabricated zero — deliberately different from `ProjectDemandRead.allocated_hours`'s own bounded-period "0 when unallocated" convention, since this axis asks whether capacity has been captured at all, not how much demand exists in one date range. +27 frontend tests (341 → 368). | [0042](adr/0042-capacity-priority-matrix.md) |
| 43 | Audit Log UI | The item Phase 41's re-audit found: `GET /api/v1/audit` (built since Phase 10, `AUDIT_READ`-gated) had zero frontend consumer. Built with **zero backend changes** (`features/audit/`, one new `/admin/audit` route, one new nav entry, gated by `can('audit.read')`): consumes the existing `GET /api/v1/audit` contract exactly — actor and since/until date-range filters only (`action`/`resource_type`, though also real server filters, are shown as table columns, not dropdowns, since both are open backend-owned vocabularies with no exposed enum — a dropdown would either duplicate that list or misrepresent completeness from a partial page). The first real pagination UI in this frontend (Previous/Next over the endpoint's existing `limit`/`offset`/`total`), since an audit trail — unlike every other "list all X" entity in this app — grows without bound. `action` renders as its raw machine code, never an invented human-readable label. No organization id in the query key — switching organizations already purges the whole query cache (established since Phase 12). +20 frontend tests (368 → 388). | [0043](adr/0043-audit-log-ui.md) |
| 44 | Audit Log — action & resource-type filtering | The small follow-up ADR 0043 itself named. Re-verified directly against current source that `action`/`resource_type` remain plain equality filters (`AuditEvent.action == action`), never substring/case-insensitive. Extends the existing Phase 43 filter bar with two new **plain text** inputs — never a dropdown, for the same reason Phase 43 already gave (`AuditAction` is an open, backend-owned vocabulary). **Zero backend changes** — both query parameters already existed. Both feed the existing `['audit-events', filters]` query key and reset pagination the same way every existing filter already does; no table or empty-state changes needed. +13 frontend tests (388 → 401). | [0044](adr/0044-audit-log-action-resource-filtering.md) |
| 45 | Risk vs. Value quadrant | The last of the PRD's five §15 visualizations, blocked since Phase 25 on two undefined concepts: project "Value" and Risk aggregation. Re-audited directly against current source (`Project` still has no value field; `Risk` still has no numeric field — `exposure` deliberately categorical; WSJF's `business_value` is one of four inputs to a different score, not standalone project value; no risk-aggregation function exists anywhere), then put to the user as a blocking Decision Report with 2-3 options per concept rather than guessed. **The user chose:** Value = the existing priority score (Phase 42's own Y-axis, reused verbatim); Risk = count of a project's open, high-exposure `Risk` records — the exact condition the existing `risk_high_exposure` Insights signal already uses, reused as a plain count, never a synthesized score. **Zero backend changes** — no bulk risk endpoint exists (ADR 0013), so `useRisksForProjects` fires one existing `GET /projects/{id}/risks` per plotted project via `useQueries`, sharing cache with the standalone Risks page. A risk count of 0 is a real plotted value (unlike Phase 42's capacity axis); a project is excluded only when its own risk fetch genuinely fails, never blocking the rest of the chart. Median-based reference lines reapply Phase 42's own technique deliberately (the only one this codebase has ever used for a descriptive quadrant). +35 frontend tests (401 → 436). Every PRD §15 visualization is now implemented. | [0045](adr/0045-risk-value-quadrant.md) |

**Tag:** [`v0.1-foundation`](https://github.com/blessingochuwa/capacityos/releases/tag/v0.1-foundation) marks Phases 0–16 complete (Phases 17–45 landed after the tag).

## Proposed future phases

None of these have a confirmed number, order, or ADR yet — each should be confirmed (with the user, per this project's established practice) before work starts, exactly as Phases 13–27 each were.

### 🔜 Proposed next: the rest of Prioritization

Phases 17-27 together shipped a deliberately reduced slice of the original Phase 17 PRD (each confirmed with the user before implementation, per CLAUDE.md §31's "smallest complete slice"). **All five of the PRD's own §15 Recharts visualizations are now implemented** — the last, Risk vs. Value quadrant, is resolved as of Phase 45 ([ADR 0045](adr/0045-risk-value-quadrant.md)): Value reuses the existing priority score (Phase 42's own definition, reused verbatim), Risk is the count of a project's open, high-exposure `Risk` records (reusing the existing `risk_high_exposure` Insights signal's own condition as a plain count) — both supplied by explicit user decision after a Phase 45 Decision Report, not derived by audit. Still named, scoped, not dropped:

- **Scenario snapshots** — a snapshot of a scenario's hypothetical (rather than baseline) ranking, within a `PortfolioSnapshot`'s conceptual reach but genuinely ambiguous: `Scenario` (unlike `PrioritizationFramework`) supports a real, non-soft delete, so what happens to a scenario snapshot when its scenario is deleted needs a product decision before this is buildable (audited and explicitly not selected for Phase 22, re-confirmed still open — directly against current code, not merely repeated — by the Phase 23 through Phase 41 audits — see ADR 0022's Context).
- A rank-over-time or toggleable score/rank variant of the Phase 24 trend chart — audited and explicitly not selected for Phase 24 (a rank trend risks conflating a project's own change with a sibling project entering/leaving the ranking, and a MoSCoW framework has no rank at all — see ADR 0024's Decision).

**Audit Log UI is resolved as of Phase 43** ([ADR 0043](adr/0043-audit-log-ui.md)) — `GET /api/v1/audit` (built since Phase 10) now has a frontend consumer at `/admin/audit`: actor and since/until filters, Previous/Next pagination over the endpoint's existing `limit`/`offset`/`total`, `action` shown as its raw machine code rather than an invented label. Zero backend changes. **Action & resource-type filtering for it is resolved as of Phase 44** ([ADR 0044](adr/0044-audit-log-action-resource-filtering.md)) — plain text inputs, never dropdowns, matching the confirmed exact-match backend semantic.

An AI explanation of a Phase 22 snapshot comparison is resolved as of Phase 23 — see [ADR 0023](adr/0023-ai-snapshot-comparison-explanation.md). A multi-snapshot score-over-time trend chart is resolved as of Phase 24 — see [ADR 0024](adr/0024-portfolio-snapshot-trend.md). The PRD's own WSJF breakdown visualization is resolved as of Phase 25 — see [ADR 0025](adr/0025-wsjf-breakdown-visualization.md). An AI interpretation of the Phase 20 scenario-vs-baseline comparison is resolved as of Phase 26 — see [ADR 0026](adr/0026-ai-scenario-priority-comparison-explanation.md). The PRD's own Priority-vs-Effort scatter is resolved as of Phase 27 — see [ADR 0027](adr/0027-priority-effort-scatter-visualization.md). The PRD's own dependency timeline is resolved as of Phase 40 — see [ADR 0040](adr/0040-dependency-timeline.md) (frontend-only, zero backend changes; `Project.start_date`/`end_date` as the axis, undated projects listed separately, `blocks` edges only, no schedule-consistency judgement). The PRD's own Capacity vs. Priority matrix is resolved as of Phase 42 — see [ADR 0042](adr/0042-capacity-priority-matrix.md) (also frontend-only, zero backend changes; capacity is total recorded allocation hours per project, priority is the existing portfolio score, median-based reference lines with a documented tie rule, a project with zero recorded allocations excluded as missing data rather than plotted at a fabricated zero). The PRD's own Risk vs. Value quadrant is resolved as of Phase 45 — see [ADR 0045](adr/0045-risk-value-quadrant.md) (also frontend-only, zero backend changes; Value = existing priority score, Risk = count of open high-exposure `Risk` records, both by explicit user decision, median-based reference lines reapplying Phase 42's own technique). See [ADR 0020](adr/0020-scenario-priority-comparison.md)'s, [ADR 0021](adr/0021-portfolio-snapshots.md)'s, [ADR 0022](adr/0022-portfolio-snapshot-comparison.md)'s, [ADR 0023](adr/0023-ai-snapshot-comparison-explanation.md)'s, [ADR 0024](adr/0024-portfolio-snapshot-trend.md)'s, [ADR 0025](adr/0025-wsjf-breakdown-visualization.md)'s, [ADR 0026](adr/0026-ai-scenario-priority-comparison-explanation.md)'s, [ADR 0027](adr/0027-priority-effort-scatter-visualization.md)'s, [ADR 0040](adr/0040-dependency-timeline.md)'s, [ADR 0042](adr/0042-capacity-priority-matrix.md)'s, and [ADR 0045](adr/0045-risk-value-quadrant.md)'s Consequences for the authoritative list.

### 📋 Proposed, unscheduled

- **Risk, Stakeholder & Prioritization Import/Export registration is resolved as of Phase 36** ([ADR 0036](adr/0036-import-export-risk-stakeholder-prioritization.md)) — `risk` (a new nullable `external_id` column), `stakeholder` (matched by its existing `(project_id, person_id)` unique constraint, no schema change), and `project_priority_score` (the human-entered scoring inputs; `PrioritizationFramework` itself deliberately excluded as a `PRIORITIZATION_MANAGE`-gated privilege-escalation risk) are all registered into the existing Phase 6 pipeline. **`ProjectDependency` (Phase 18) Import/Export registration is resolved as of Phase 37** ([ADR 0037](adr/0037-import-export-project-dependency.md)) — its existing `(from_project_id, to_project_id, dependency_type)` unique constraint already supplied a complete natural key, so 0 migrations were needed; `BLOCKS`-only cycle detection reuses the same `detects_cycle` function the direct API already calls, via a cross-row batch-simulation technique mirroring WorkingSchedule's overlap pre-check. **`PortfolioSnapshot` (Phase 21) is intentionally neither importable nor exportable — resolved as of Phase 38** ([ADR 0038](adr/0038-portfoliosnapshot-export-capability.md)). Phase 37 settled import (immutable, append-only like `AuditEvent`, "deliberately NOT read back as an input to any live computation," `create()` takes only a `framework_id` — no user-supplied content to import) and deferred export to its own dedicated audit. Phase 38 resolved that: export is rejected on **product** grounds, not merely architectural ones — `AuditEvent` (the entity's own named precedent) has never been exportable in this codebase; the PRD describes the purpose as in-app "trend/history," never backup/download; ADR 0006's charter is about operational source-data portability, not a derived artifact; and the "see the trend" need is already served by Phase 24's chart. No `importable`/`exportable` capability architecture was built, since the one entity that would have needed it turns out to need neither. This is a deliberate, documented non-registration on both sides, not an unaudited gap.
- **Org-wide cross-project Risk and Stakeholder registers** — both entities are currently nested under one Project only; a register spanning every project in an organization was named but not built (ADR 0013/0014 Consequences).
- **Membership- / user- / organization-management UI** — the membership roster/role/status slice is **done** as of Phase 28 ([ADR 0028](adr/0028-membership-management-ui.md)), the `User`-account create/disable/re-enable slice as of Phase 29 ([ADR 0029](adr/0029-user-account-management-ui.md)), and organization **rename** as of Phase 30 ([ADR 0030](adr/0030-organization-settings-ui.md)) — all frontend-only. The backend deactivation lifecycle was made **safe and reversible** in Phase 31 ([ADR 0031](adr/0031-organization-deactivation-safety.md)) and its **frontend** shipped in Phase 32 ([ADR 0032](adr/0032-organization-deactivation-reactivation-ui.md), frontend-only) — a Deactivation section and an inactive-org recovery panel that surface the backend's 422/403/404 verbatim. Phase 33 ([ADR 0033](adr/0033-global-inactive-organization-awareness.md)) closed the two Phase-32 follow-ups: `OrganizationSummary.is_active` on `/auth/me`, a persistent shell `InactiveOrganizationBanner`, and switcher filtering of deactivated orgs. The organization lifecycle UX is now complete for the current backend surface. The **account-directory search/filter box** is done as of Phase 34 ([ADR 0034](adr/0034-account-directory-search-filter.md)) — `q`/`status` query params, filtered server-side, global scope preserved. The **`USER_WRITE` organization-scoping question** is resolved as of Phase 35 ([ADR 0035](adr/0035-user-write-global-scope.md)) — the user was asked directly (no default was assumed), and chose to keep `USER_WRITE` global, matching `USER_READ`'s already-explicit global scope (ADR 0012 Decision 8). No code changed; the decision is now documented rather than open.
- **External integrations foundation** (CLAUDE.md §22) — Slack, Jira, Linear, Asana, ClickUp, Google Calendar, via an isolated adapter layer (`External System → Integration Adapter → CapacityOS Internal Model → Domain Engine`) so vendor-specific logic never spreads into the domain layer. Deliberately not started in any phase through 17.
- **SSO / OAuth / external identity provider** — deferred since Phase 10 (session-cookie auth was chosen specifically because a self-hosted option was sufficient for that phase's scope).
- **Billing / subscription** — no concept exists anywhere in the schema or domain model (ADR 0012 Consequences).
- **Organization hierarchies / sub-organizations** — every organization is currently a flat, independent tenant (ADR 0012 Consequences).
- **Chrome extension** (CLAUDE.md §23) — explicitly deferred; when built, it must call the CapacityOS API rather than duplicate any business logic client-side.
- **PostgreSQL verification of the Phase 15 last-owner concurrency guard** — the atomic-guarded-`UPDATE` technique is expected to hold under PostgreSQL's isolation levels (it's portable SQL), but this was verified against SQLite's single-file writer serialization only; a real PostgreSQL deployment should confirm it before being relied on as a production guarantee (ADR 0015 Consequences).
- **Team→Project access-grant inheritance, Person-keyed instance scoping (WorkingSchedule/AvailabilityException/PersonSkill), Scenario instance scoping** — audited and *deliberately retained* as role-only by Phase 16 (ADR 0016), not merely unbuilt. Re-opening any of these requires a new, explicit product requirement naming the missing ownership concept CLAUDE.md would need to define first — not a default next step.

## Keeping this document current

Update this file whenever CLAUDE.md §39 gains a new confirmed phase (move it from "Proposed" to the completed table, link its ADR) or a proposed item's scope changes. If a phase is dropped or superseded, say so explicitly here rather than silently deleting the row — matching CLAUDE.md's own "do not silently remove deferred items" convention (§39's amendment history is the model to follow).
